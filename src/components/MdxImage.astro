---
import { withBase } from '../lib/urls';

interface Props {
  src: string;
  alt?: string;
  [key: string]: unknown;
}
const { src, alt, ...rest } = Astro.props;
---
<img src={withBase(src)} alt={alt} {...rest} />
